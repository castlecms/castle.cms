# Scripts

The scripts in this folder are not intended to be run within a CastleCMS/Plone app context.

They are simply one-off python scripts to be run with a Python interpreter, and possibly some
dependencies which will have to be identified per-script.

Likely python 3.9+ compat.
